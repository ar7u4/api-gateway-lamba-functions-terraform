Creating an end-to-end setup using Terraform to create an API Gateway with a mock response and connecting it to a Lambda function. 
The Lambda function will handle the requests and provide the responses. 

